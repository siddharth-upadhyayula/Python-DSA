class Solution:
    def countSeniors(self, details: List[str]) -> int:
    // initialise count
        count = 0
        for i in details:
      // split the string for getting the age and convert it to int
            age = int(i[11:13])
            if age>60:
                count+=1
        return count
